FROM mongo:latest

ARG MONGO_USER
ARG MONGO_PASS

ENV MONGO_INITDB_ROOT_USERNAME=${MONGO_USER}
ENV MONGO_INITDB_ROOT_PASSWORD=${MONGO_PASS}
ENV MONGO_INITDB_DATABASE=db_points

COPY ./init-scripts/ /docker-entrypoint-initdb.d/

EXPOSE 27017